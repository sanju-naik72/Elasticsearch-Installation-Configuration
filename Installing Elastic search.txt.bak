Install ubuntu with
-------> t2.medium and 20GB EBS
		 Security Group as required OR give all traffic as per now
		 
Install Java
		
		sudo apt-get update
		sudo apt-get install -y openjdk-8-jdk


Install Nginx on the device

		sudo apt-get update
		sudo apt-get -y install nginx
 
Downloading and installing Elasticsearch

		wget https://artifacts.elastic.co/downloads/elasticsearch/elasticsearch-7.2.0-amd64.deb
		sudo dpkg -i elasticsearch-7.2.0-amd64.deb
		

Downloading and installing Kibana 
		wget http://artifacts.elastic.co/downloads/kibana/kibana-7.2.0-amd64.deb
		sudo dpkg -i kibana-7.2.0-amd64.deb